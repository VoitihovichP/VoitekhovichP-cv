import React, { FC } from 'react';
import * as S from './styled';

const StartInfo: FC = () => {
  return (
    <S.StartInfoContainer>
      <h1>Start Info</h1>
    </S.StartInfoContainer>
  );
};

export default StartInfo;
